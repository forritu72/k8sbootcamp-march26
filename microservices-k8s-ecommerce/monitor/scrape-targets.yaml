# =============================================================================
# DEPRECATED - kept as a placeholder so `kubectl apply -f ./monitor/` succeeds.
# =============================================================================
# Pod-level scraping is now driven by `prometheus.io/scrape` annotations set on
# each service's pod template in the Helm chart (see helm-cnpg-vault/templates/
# *-service.yaml). The `kubernetes-pods` job in prometheus-configmap.yaml picks
# them up automatically via Kubernetes SD.
#
# The old static-target Services in `default` namespace were removed - they
# pointed to the wrong namespace and contributed nothing.
# =============================================================================
